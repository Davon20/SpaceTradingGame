using System;
using System.Collections.Generic;
using System.Text;
using System.IO;

namespace SpaceTradingGame
{
    class Planets //Allen is the object that goes to every planet. Allen.Earth = new Plot(); 
    {
        public void Earth() //if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons, Count Trades
        {
            Console.WriteLine("Hello");
        }
        public void alphaCenturia()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons, Count Trades
        {
            //Needs Terminal Item List
        }
        public void DathomirIntro()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons, Count Trades
        {
            //Carrie, Chuy, Transportation Service Guy, Scavenger
        }
        public void Dathomir()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons, Count Trades
        {
            //Needs Terminal Item List
        }
        public void Tatooine()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons, Count Trades
        {
            //Needs Terminal Item List
        }
        public void Alderan()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons, Count Trades
        {
            //Needs Terminal Item List
        }
        public void Sentinel()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons, Count Trades
        {
            //Needs Terminal Item List
        }
        public void September()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons, Count Trades
        {
            //Needs Terminal Item List
        }
    }
}