using System;
using System.Collections.Generic;
using System.Text;
using System.IO;

namespace SpaceTradingGame
{
    class Planets //Allen is the object that goes to every planet. Allen.Earth = new Plot(); 
    {
        public string name;

        public static void Earth() //if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons
        {
            Console.WriteLine("Hello");
            // REquires if statement: Status.tradeCounter();
        }
        public static void alphaCenturia()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons
        {
            //Needs Terminal Item List
        }
        public static void DathomirIntro()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons
        {
            //Carrie, Chuy, Transportation Service Guy, Scavenger
        }
        public static void Dathomir()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons
        {
            //Needs Terminal Item List
        }
        public static void Tatooine()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons
        {
            //Needs Terminal Item List
        }
        public static void Alderan()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons
        {
            //Needs Terminal Item List
        }
        public static void Sentinel()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons
        {
            //Needs Terminal Item List
        }
        public static void September()//if(tradeMenu(pressed b) -> Needs to display bankRoll, Buy/Sell Buttons
        {
            //Needs Terminal Item List
        }
    }
}